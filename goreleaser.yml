builds:
  - id: "wlistproxy"
    flags:
      - -v
      - -trimpath
    ldflags:
      - -s -w
    goarch:
      - amd64
      - arm64
    goos:
      - linux
    env:
      - CGO_ENABLED=0

archives:
  - format: binary
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
